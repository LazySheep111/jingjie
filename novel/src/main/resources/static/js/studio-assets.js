(async () => { const grid=document.getElementById('assetGrid'); try { const data=await studioApi.request('/api/visual-assets/library?page=1&pageSize=60'); const items=Array.isArray(data)?data:(data.records||[]); grid.innerHTML=items.length?items.map(item=>`<article class="asset-row"><strong>${item.assetName||'未命名资产'}</strong><span>${item.assetType||'资产'}</span></article>`).join(''):'暂无资产。'; } catch(e){grid.textContent=`加载失败：${e.message}`;} })();
