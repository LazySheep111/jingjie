(async () => { const list=document.getElementById('projectList'); try { const data=await studioApi.request('/api/novel/history?page=1&pageSize=30'); const items=Array.isArray(data)?data:(data.records||[]); list.innerHTML=items.length?items.map(item=>`<article class="asset-row"><strong>${item.novelTitle||'未命名作品'}</strong><a href="/studio/workbench.html?novelId=${item.novelId}&chapterNum=1">进入工作台</a></article>`).join(''):'暂无项目。'; } catch(e){list.textContent=`加载失败：${e.message}`;} })();
